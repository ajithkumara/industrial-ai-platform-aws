# ---- domain core (cloud-free) ----
pydantic>=2.0
pyyaml>=6.0
python-dotenv==1.2.2
numpy>=1.26
scipy>=1.11
Faker

# ---- AWS ingestion (Milestone 2) ----
boto3>=1.34

# ---- NATS bridge (optional runtime) ----
nats-py>=2.6.0

# ---- tests ----
pytest>=8.0
moto>=5.0
